# --- 数据处理与科学计算 (Data Handling & Scientific Computing) ---
# 数据分析与处理的核心工具。
pandas==2.2.2
# Python 科学计算的基础库，几乎所有库都依赖它。
numpy==1.26.4

# --- 传统机器学习模型 (Classical Machine Learning) ---
# 强大的机器学习库，用于数据预处理、基线模型和评估。1.5.0版本与Python 3.11兼容性良好。
scikit-learn==1.5.0
# 高效的梯度提升框架，用于树模型分支。
lightgbm==4.3.0

# --- 模型解释性分析 (Model Interpretability) ---
# 用于模型解释，支持树模型和深度学习模型，是现代ML项目不可或缺的工具。
shap==0.45.0

# --- 数据可视化 (Data Visualization) ---
# 基础绘图库。
matplotlib==3.9.0
# 基于Matplotlib的高级统计数据可视化库。
seaborn==0.13.2

# --- 辅助工具 (Utilities) ---
# 进度条工具，方便在训练或数据处理时监控进度。
tqdm==4.66.4
# 用于读取 .env 文件中的环境变量，方便管理配置。
python-dotenv==1.0.1
# 在Jupyter环境中进行交互式开发时需要。
ipykernel
openpyxl==3.1.2

# --- 科学计算扩展 (Scientific Computing Extensions) ---
# 科学计算库，提供优化、积分、线性代数等功能。
scipy==1.13.1


# --- 时序数据专用库 (Time Series Specific Libraries) ---
# 时序数据机器学习库，提供DTW、聚类等算法
tslearn==0.6.3
# 时序分析和转换库
pyts==0.13.0

# --- 高级可视化 (Advanced Visualization) ---
# 交互式数据可视化库
plotly==5.22.0

# --- 超参数优化 (Hyperparameter Optimization) ---
# 自动超参数优化框架
optuna==3.6.1

imbalanced-learn

# --- 深度学习框架 (Deep Learning) ---
# PyTorch深度学习框架，用于LSTM、GRU、CNN-LSTM模型
torch==2.3.0
torchvision==0.18.0

# --- ONNX模型导出 (ONNX Export) ---
# ONNX模型格式支持，用于跨平台部署
onnx>=1.14.0
onnxruntime>=1.15.0
# 树模型ONNX转换
skl2onnx>=1.16.0
onnxmltools>=1.12.0
# GPU加速推理（可选）
# onnxruntime-gpu>=1.15.0

# --- 分布式计算 (Distributed Computing) ---
# 分布式机器学习框架，用于大规模超参数搜索
#ray[tune]==2.24.0